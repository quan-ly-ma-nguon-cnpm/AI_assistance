@{
<<<<<<< HEAD
         ViewData["Title"] = "AI Application Chatbot";
     }

     <!DOCTYPE html>
     <html lang="vi">
     <head>
         <meta charset="UTF-8">
         <meta name="viewport" content="width=device-width, initial-scale=1.0">
         <title>@ViewData["Title"]</title>
         <style>
             body {
                 margin: 0;
                 font-family: Arial, sans-serif;
                 display: flex;
                 height: 100vh;
                 background-color: #f5f7fa;
                 overflow: hidden;
             }

             .sidebar {
                 width: 60px;
                 background-color: #e6f0fa;
                 display: flex;
                 flex-direction: column;
                 align-items: center;
                 padding: 20px 0;
             }

             .logo img {
                 width: 40px;
                 height: 40px;
             }

             .menu {
                 margin-top: 20px;
                 display: flex;
                 flex-direction: column;
                 gap: 15px;
             }

             .menu-btn {
                 background: none;
                 border: none;
                 font-size: 20px;
                 cursor: pointer;
                 padding: 10px;
                 border-radius: 5px;
             }

             .menu-btn:hover {
                 background-color: #d0e1f9;
             }

             .chat-container {
                 flex: 1;
                 display: flex;
                 flex-direction: column;
                 align-items: center;
                 padding: 20px;
             }

             .chat-header {
                 text-align: center;
             }

             .chat-logo {
                 width: 40px;
                 height: 40px;
             }

             .chat-header h1 {
                 font-size: 24px;
                 margin: 10px 0;
                 color: #333;
             }

             .chat-header p {
                 font-size: 16px;
                 color: #666;
             }

             .chat-messages {
                 flex: 1;
                 width: 50%;
                 overflow-y: auto; /* Đảm bảo cuộn dọc */
                 overflow-x: hidden; /* Ẩn cuộn ngang */
                 margin-bottom: 20px;
                 padding: 10px;
                 display: flex;
                 flex-direction: column;
                 gap: 10px;
                 scrollbar-width: thin; /* Tùy chỉnh độ rộng thanh cuộn cho Firefox */
                 scrollbar-color: #888 #f5f7fa; /* Màu thanh cuộn và nền cho Firefox */
             }

             /* Tùy chỉnh thanh cuộn cho Chrome, Safari, Edge */
             .chat-messages::-webkit-scrollbar {
                 width: 8px; /* Độ rộng thanh cuộn */
             }

             .chat-messages::-webkit-scrollbar-track {
                 background: #f5f7fa; /* Màu nền của thanh cuộn */
                 border-radius: 4px;
             }

             .chat-messages::-webkit-scrollbar-thumb {
                 background-color: #888; /* Màu của thanh cuộn */
                 border-radius: 4px;
                 border: 2px solid #f5f7fa; /* Khoảng cách giữa thanh cuộn và viền */
             }

             .chat-messages::-webkit-scrollbar-thumb:hover {
                 background-color: #555; /* Màu khi hover */
             }

             .message {
                 max-width: 70%;
                 padding: 10px;
                 border-radius: 10px;
                 word-wrap: break-word;
             }

             .message.user {
                 background-color: #007bff;
                 color: white;
                 align-self: flex-end;
             }

             .message.bot {
                 background-color: #e6f0fa;
                 color: #333;
                 align-self: flex-start;
             }

             .chat-input {
                 display: flex;
                 align-items: center;
                 gap: 10px;
                 background-color: #e6f0fa;
                 padding: 10px;
                 border-radius: 10px;
                 width: 50%;
             }

             .chat-input input {
                 flex: 1;
                 padding: 10px;
                 border: none;
                 border-radius: 5px;
                 background-color: transparent;
                 outline: none;
             }

             .action-btn {
                 background-color: #ffffff;
                 border: 1px solid #ddd;
                 padding: 5px 10px;
                 border-radius: 5px;
                 cursor: pointer;
             }

             .action-btn:hover {
                 background-color: #f0f0f0;
             }

             .send-btn {
                 background-color: #007bff;
                 color: white;
                 border: none;
                 padding: 10px;
                 border-radius: 50%;
                 cursor: pointer;
             }

             .send-btn:hover {
                 background-color: #0056b3;
             }
         </style>
     </head>
     <body>
         <div class="sidebar">
             <div class="logo">
                 <img src="~/images/logo.png" alt="Logo">
             </div>
             <div class="menu">
                 <button class="menu-btn">📄</button>
                 <button class="menu-btn">➕</button>
                 <button class="menu-btn">💬</button>
             </div>
         </div>
         <div class="chat-container">
             <div class="chat-header">
                 <img src="~/images/chat-logo.png" alt="Chat Logo" class="chat-logo">
                 <h1>Xin chào, tôi là Hannah.</h1>
                 <p>Tôi có thể giúp gì cho bạn hôm nay?</p>
             </div>
             <div class="chat-messages" id="chatMessages">
                 <!-- Tin nhắn sẽ được thêm động -->
             </div>
             <div class="chat-input">
                 <input type="text" id="messageInput" placeholder="Nhắn tin cho Hannah" onkeypress="if(event.key === 'Enter') sendMessage();">
                 <button class="action-btn">DeepThink (1+)</button>
                 <button class="action-btn">Search</button>
                 <button class="send-btn" onclick="sendMessage()">➤</button>
             </div>
         </div>

         <script>
             function sendMessage() {
                 const input = document.getElementById('messageInput');
                 const chatMessages = document.getElementById('chatMessages');

                 const userMessage = input.value.trim();
                 if (!userMessage) return;

                 // Hiển thị tin nhắn người dùng
                 const userDiv = document.createElement('div');
                 userDiv.className = 'message user';
                 userDiv.textContent = userMessage;
                 chatMessages.appendChild(userDiv);

                 // Gửi tin nhắn đến server
                 fetch('/Home/SendMessage', {
                     method: 'POST',
                     headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
                     body: 'message=' + encodeURIComponent(userMessage)
                 })
                 .then(response => response.json())
                 .then(data => {
                     if (data.success) {
                         const botDiv = document.createElement('div');
                         botDiv.className = 'message bot';
                         botDiv.textContent = data.botMessage;
                         chatMessages.appendChild(botDiv);
                     }
                 })
                 .catch(error => console.error('Error:', error));

                 input.value = '';
                 chatMessages.scrollTop = chatMessages.scrollHeight; // Cuộn xuống tin nhắn mới nhất
             }
         </script>
     </body>
     </html>
=======
    ViewData["Title"] = "Home Page";
}
>>>>>>> 1c0d28298517a5d9b8a2cf25cf1991a188448798
